<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/app_color_background"
        tools:context=".MainActivity">

    <ImageView
            android:id="@+id/app_icon"
            android:layout_width="@dimen/app_fifteen"
            android:layout_height="@dimen/app_fifteen"
            android:layout_marginTop="@dimen/app_seven"
            android:src="@drawable/ic_hello_my_name_is"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"/>

    <TextView
            android:id="@+id/app_user_name"
            style="@style/HelloMyNameIs.Text.Title.Bold"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/app_seven"
            android:gravity="center"
            app:layout_constraintEnd_toEndOf="@id/app_icon"
            app:layout_constraintStart_toStartOf="@id/app_icon"
            app:layout_constraintTop_toTopOf="@id/app_icon"
            app:layout_constraintBottom_toBottomOf="@id/app_icon"/>

    <Button
            android:id="@+id/get_profile_button"
            style="@style/HelloMyNameIs.Button"
            android:layout_width="0dp"
            android:layout_height="@dimen/app_default_button_height"
            android:layout_margin="@dimen/app_three"
            android:enabled="false"
            android:text="@string/main_get_profile"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

</android.support.constraint.ConstraintLayout>